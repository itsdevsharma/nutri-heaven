# Public API origin used at build time by Vite. Do not include a trailing slash.
# Local development:
VITE_API_URL=http://localhost:3000
# Vercel production example (replace with the public HTTPS API origin):
# VITE_API_URL=https://api.example.com
