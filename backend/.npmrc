# Shared pnpm settings. Committed on purpose — no secrets belong in this file.
# Registry auth tokens go in your personal ~/.npmrc, never here.

# Install missing peer dependencies instead of failing the build.
auto-install-peers=true

# Refuse to install on a Node version the repo does not support.
engine-strict=true
